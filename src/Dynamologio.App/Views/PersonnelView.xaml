<UserControl x:Class="Dynamologio.App.Views.PersonnelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource CanvasBgBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header & Action Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Μητρώο Προσωπικού &amp; Στοιχεία Δύναμης" FontSize="16" FontWeight="Bold" Foreground="{StaticResource HeaderDarkBrush}" />
                <TextBlock Text="Διαχείριση ονομαστικών στοιχείων, βαθμών, τοποθετήσεων και ιστορικού μεταβολών" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Προσθήκη Προσωπικού" Command="{Binding AddPersonCommand}" Style="{StaticResource PrimaryButton}" Height="34" />
            </StackPanel>
        </Grid>

        <!-- Main Master/Detail Layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" MinWidth="320" />
            </Grid.ColumnDefinitions>

            <!-- Left: Personnel List & Search -->
            <Border Grid.Column="0" Style="{StaticResource CardContainer}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Search & Filter Bar -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="130" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" />
                        <ComboBox Grid.Column="2" SelectedValue="{Binding SelectedCategoryFilter}" SelectedValuePath="Content" VerticalAlignment="Center">
                            <ComboBoxItem Content="Όλοι" IsSelected="True" />
                            <ComboBoxItem Content="Στελέχη" />
                            <ComboBoxItem Content="Οπλίτες" />
                        </ComboBox>
                    </Grid>

                    <!-- Table -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding DisplayPersonnel}" SelectedItem="{Binding SelectedPerson}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Βαθμός" Binding="{Binding Rank.ShortName}" Width="75" />
                            <DataGridTextColumn Header="Επώνυμο &amp; Όνομα" Binding="{Binding Person.FullName}" Width="*" />
                            <DataGridTextColumn Header="ΑΣΜ" Binding="{Binding Person.MilitaryServiceNumber}" Width="85" />
                            <DataGridTextColumn Header="Μονάδα/Λόχος" Binding="{Binding Unit.Name}" Width="110" />
                            <DataGridTextColumn Header="Κατάσταση" Binding="{Binding StatusDisplayLabel}" Width="95" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Right: Detail Panel for Selected Person -->
            <Border Grid.Column="2" Style="{StaticResource CardContainer}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Καρτέλα Προσωπικού" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource HeaderDarkBrush}" Margin="0,0,0,10" />

                        <!-- Identity Card -->
                        <Border Background="{StaticResource SurfaceElevatedBrush}" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedPerson.Person.FullName, FallbackValue='Επιλέξτε πρόσωπο'}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource HeaderDarkBrush}" />
                                <TextBlock Text="{Binding SelectedPerson.Rank.Name}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,4" />
                                
                                <Grid Margin="0,6,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="ΑΣΜ:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{Binding SelectedPerson.Person.MilitaryServiceNumber, FallbackValue='-'}" FontWeight="SemiBold" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Τμήμα/Λόχος:" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{Binding SelectedPerson.Unit.Name, FallbackValue='-'}" FontWeight="SemiBold" />
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- Actions for Selected Person -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                            <Button Content="Επεξεργασία" Command="{Binding EditPersonCommand}" Style="{StaticResource SecondaryButton}" Width="105" Margin="0,0,8,0" />
                            <Button Content="Αρχειοθέτηση" Command="{Binding ArchivePersonCommand}" Style="{StaticResource DangerButton}" Width="110" />
                        </StackPanel>

                        <!-- Absence History -->
                        <TextBlock Text="Ιστορικό Μεταβολών &amp; Αδειών" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource HeaderDarkBrush}" Margin="0,0,0,6" />
                        <DataGrid ItemsSource="{Binding SelectedPersonAbsenceHistory}" Height="120" Margin="0,0,0,12">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Από" Binding="{Binding StartAt, StringFormat='dd/MM/yy'}" Width="65" />
                                <DataGridTextColumn Header="Έως" Binding="{Binding EndAtExclusive, StringFormat='dd/MM/yy'}" Width="65" />
                                <DataGridTextColumn Header="Διαταγή" Binding="{Binding ReferenceDocument}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Service History -->
                        <TextBlock Text="Ιστορικό Υπηρεσιών" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource HeaderDarkBrush}" Margin="0,0,0,6" />
                        <DataGrid ItemsSource="{Binding SelectedPersonServiceHistory}" Height="100">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ημ/νία" Binding="{Binding ServiceDate, StringFormat='dd/MM/yy'}" Width="65" />
                                <DataGridTextColumn Header="Ώρες" Binding="{Binding StartDateTime, StringFormat='HH:mm'}" Width="55" />
                                <DataGridTextColumn Header="Τοποθεσία" Binding="{Binding DutyLocation}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
