<UserControl x:Class="Dynamologio.App.Views.PersonnelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="950">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Search Bar & Filters -->
        <Border Grid.Row="0" Style="{StaticResource CardContainer}" Margin="0,0,0,16" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🔍 Αναζήτηση:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,16,0" />

                <TextBlock Grid.Column="2" Text="Κατηγορία:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                <ComboBox Grid.Column="3" SelectedValue="{Binding SelectedCategoryFilter}" SelectedValuePath="Content" Width="130" VerticalAlignment="Center">
                    <ComboBoxItem Content="Όλοι" IsSelected="True" />
                    <ComboBoxItem Content="Στελέχη" />
                    <ComboBoxItem Content="Οπλίτες" />
                </ComboBox>

                <Button Grid.Column="5" Content="➕ Προσθήκη" Style="{StaticResource PrimaryButton}" Command="{Binding AddPersonCommand}" Margin="0,0,8,0" />
                <Button Grid.Column="6" Content="✏️ Επεξεργασία" Style="{StaticResource SecondaryButton}" Command="{Binding EditPersonCommand}" Margin="0,0,8,0" />
                <Button Grid.Column="7" Content="📦 Αρχειοθέτηση" Style="{StaticResource DangerButton}" Command="{Binding ArchivePersonCommand}" />
            </Grid>
        </Border>

        <!-- Personnel DataGrid -->
        <Border Grid.Row="1" Style="{StaticResource CardContainer}">
            <DataGrid ItemsSource="{Binding DisplayPersonnel}" SelectedItem="{Binding SelectedPerson}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ΑΣΜ" Binding="{Binding Person.MilitaryServiceNumber}" Width="100" />
                    <DataGridTextColumn Header="Βαθμός" Binding="{Binding Rank.ShortName}" Width="110" />
                    <DataGridTextColumn Header="Επώνυμο" Binding="{Binding Person.LastName}" Width="170" />
                    <DataGridTextColumn Header="Όνομα" Binding="{Binding Person.FirstName}" Width="150" />
                    <DataGridTextColumn Header="Μονάδα / Λόχος" Binding="{Binding Unit.Name}" Width="140" />
                    <DataGridTextColumn Header="Ειδικότητα" Binding="{Binding Person.Specialty}" Width="130" />
                    <DataGridTextColumn Header="Τρέχουσα Κατάσταση" Binding="{Binding StatusDisplayLabel}" Width="160" />
                    <DataGridTextColumn Header="Επιστροφή" Binding="{Binding ReturnDisplayLabel}" Width="110" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
